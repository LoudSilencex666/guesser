const x = 0;
const y = 0;
const radius = 1;
const strokeWidth = 1.5;
const rgba = [];

const sprites = {
    circles : [],
    credits : [],
    spritesCounter : 0
};

const imageRadius = 1.5;
const imageStrokeWidth = 3;

export { x, y, radius, strokeWidth, rgba, sprites, imageStrokeWidth, imageRadius };